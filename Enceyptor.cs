using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DESCSharpProject
{
    class Enceyptor
    {
        private static string Encrypt(string plainTextMessage)
        {
            byte[] messageBytes = ASCIIEncoding.ASCII.GetBytes(plainTextMessage);

            return "";
        }
    }
}
